import React from 'react'
import { Text } from 'react-native'
import Search from '../../../components/Search'

export default function LateTimeScreen({ navigation }) {
  return (
    <Search title={'Nhập tên / email nhân viên'}/>
  )
}
