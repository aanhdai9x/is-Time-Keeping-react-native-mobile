import React from 'react'
import { Text } from 'react-native'
import Search from '../../../components/Search'

export default function OnTimeScreen({ navigation }) {
  return (
    <Search title={'Nhập tên / email nhân viên'}/>
  )
}
